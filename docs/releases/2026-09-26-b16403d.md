# 2026-09-26 b16403d

- GitHub 커밋: `b16403df6ded5e67794f3d05e0939c9d22697620` (`merged`, #124). 묶음 13이다. 직전 게시 8c22f0e 뒤 #116~#119와 #124가 들어갔다.
  - 제품 변경 1: 트랙 R R1b 모집 팩트시트·R2 모집 광고 가드레일, 소유자 가맹 모집 스위치(#117).
  - 제품 변경 2: 비가맹 브랜드 원장 점검 복원(#118). 묶음 13 게시 전 점검에서 찾은 회귀다.
  - 제품 변경 3: R3a 캠페인 가맹 모집 목적(objective)(#124).
  - 가맹 기능은 모두 `r_franchise`(기본 꺼짐)가 켜져야 쓸 수 있다. objective가 없는 캠페인의 동작과 모델 제출 바이트는 그대로다.
  - 나머지는 문서다(#116, #119).
- 소스 tree: `68bfa40b1852bba6434da8f98abb194f4dc49dfd`
- 승인: 대표 결정(2026-09-26, "Gpt복구되었으니 지금까지 개발한것들 한번 게하고 다음진행하자"). 게시 SHA 선택은 2026-09-25에 개발 도구에 위임됐다. 보류했던 `b0ef304`는 따로 게시하지 않고 이 묶음에 포함됐다.
- 게시 경로: 자동 게시([게시 절차 8절](../PUBLISH.ko.md#8-자동-게시-chatgpt-예약-작업))로 했다. 세 번째 요청이자 첫 성공이다.
  - 요청: PR #126, 라벨 `sites-publish`, 지시문 커밋 `650c4a0` push(02:05:56 UTC)
  - 지시문: [`docs/publish/b16403d.md`](../publish/b16403d.md). 파일 목록·기대 해시 방식이다.
  - 시작 tree `02260ba`(8c22f0e), 53줄(추가 10·수정 43·삭제 0). 임시 index 모의 적용으로 목표 tree 재현을 미리 확인했다(passed · real).
- Sites 버전 36 / deployment `appgdep_6ab72a5f43f88191831dac1c39bbdac7` succeeded / Sites 커밋 `34a5e8c783aa4f323f3d578808d6647cfe0c4ebb`
  - 게시 실행: 11:06~11:14 KST(약 8분)
  - 결과 댓글: 02:14 UTC
- D1 migration: 없음. 새 필수 환경변수 없음. 의존성 파일 변경 없음.
- 사전 점검: 크레딧·주간 한도 수치는 자동 게시라 기록되지 않는다(8절).

## 결과
| 단계 | 결과 | 근거 | 증거 |
|---|---|---|---|
| 요청 조건(라벨·본문 파싱·지시문 목표 = `PUBLISH-TARGET`) | passed | real | #126 결과 댓글 |
| main 포함 | passed | real | compare main...target = behind(ahead 0 / behind 3) |
| 대상 CI | passed | real | run 36209066178 success |
| 다른 게시 없음 | passed | real | 최신 버전 35 succeeded, 게시 직전 새 버전 없음 |
| Sites 작업 사본 시작 tree | passed | real | `git status --porcelain` 빈 값, tree `02260ba…` |
| 파일 적용·blob 검사 | passed | real | 53/53 일치, 불일치 0, 다시 받은 파일 0 |
| `git write-tree` | passed | real | `68bfa40…` = 목표 tree |
| 설치·빌드 | passed | real | `pnpm install --frozen-lockfile`, `COLLECTIVE_SOURCE_TREE` 빌드 exit 0, `TREE_EMBEDDED`, 빌드 뒤 clean·tree 일치 |
| 게시 | passed | real | `save_site_version` → `deploy_site_version`, Sites 버전 36 succeeded |
| 게시 뒤 설정 | passed | real | public 유지, 접근 revision 2, 환경 revision 4, 환경변수·D1·R2 변경 없음 |
| `/api/version` tree 비교(버전 36) | not_run | | 예약 작업은 소유자 브라우저 세션이 없다. 소유자가 확인하기 전에 버전 37로 교체됐다 |
| `/api/version` tree 비교(버전 37, b16403d 포함) | passed | real | 대표 소유자 화면 200, tree `4f3b130…` = d721017 tree(02:3x UTC). Codex 세션도 02:22 UTC 경 같은 결과를 기록했다 |
| b16403d main CI | passed | real | verify·e2e-smoke success |

- 배포 상태: `published` (Sites 버전 36). 이 버전 자체의 `runtime-verified`는 확인하지 못했다.
  - 3분 뒤(02:17:35 UTC) Codex 세션이 `d721017`(#127, b16403d를 포함)을 Sites 버전 37로 게시했다.
  - Codex 세션은 품질 콘솔의 운영 상태 조회로 d721017 tree `4f3b130…`을 확인해 `runtime-verified`로 기록했다(PR #128의 `docs/releases/2026-09-26-d721017.md`, 이 기록 작성 때 병합 전).
  - 그래서 b16403d의 코드는 버전 37 안에서 실행 검증됐다. 버전 36 단독 실행 검증은 그 확인으로 대신하지 않는다.
  - 대표 확인(2026-09-26 02:3x UTC, real): 소유자 화면의 같은 `/api/version` 요청이 HTTP 200이었고, tree는 `4f3b13022cbcfb256f85a4473db54bc10d347d77`(버전 37, d721017)이다. `68bfa40…`는 교체된 버전 36의 tree라 지금 시점의 `runtime-verified`로 소급 기록하지 않는다(대표 판단). 주소창 직접 접근 오류는 남아 있고, 정상 조회 경로로 검증했다.
- 롤백 대상: 트랙 R 코드 문제면 먼저 `r_franchise`를 끈다(기본 꺼짐). 코드 롤백이 필요하면 운영이 버전 37이므로 d721017 기준으로 판단한다(8c22f0e로 되돌리면 #127도 빠진다).
- 게시 뒤 감시(결정 4): 24~72시간 동안 5xx, Workers CPU, `invalid_output` 비율, 캡션·발행 409 비율을 본다. 가맹 게이트는 가맹 프로필 브랜드와 objective 캠페인에만 적용된다.
- 자동 게시 관찰: 예약 작업은 성공 뒤 `sites-published`를 붙였지만 `sites-publish`를 떼지 않았다. 개발 도구가 02:2x UTC에 `sites-publish`를 뗐다. 라벨이 남아 있으면 다음 push가 게시 작업을 다시 실행한다.
