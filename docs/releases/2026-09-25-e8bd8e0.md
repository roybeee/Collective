# 2026-09-25 e8bd8e0

- GitHub 커밋: `e8bd8e0724de8427cffdaa80d723583f97814347` (`merged`, #112). 묶음 11이다. 직전 게시 fa9da73 뒤 #108~#112가 들어갔다.
  - 제품 변경 1: 운영 계약 읽기(#112, `lib/role-output.ts`). JSON 끝 여분 괄호를 허용하고, 재질문 판정 오탐을 고쳤다. 두 결함은 정상 작업물을 `invalid_output`으로 거절하던 것이다.
  - 제품 변경 2: 현장 스킬 적용 조건과 문구(#111). 지점 연결·네이버 플레이스 캠페인에 붙이고, 방문 예약을 더했다.
  - 측정 도구: 채점기 `+local-rerun+contract-read`, 규제 사전 `.13`(#110·#111·#112).
- 소스 tree: `d8eacc92bd85525f93a67a1141b25d4fd0a0e8ca`
- 승인: 대표가 품질 계획 v2의 게시 SHA 선택을 위임했다(2026-09-25). 게시 실행은 대표가 했다.
- 게시 경로: 파일 목록·기대 해시 방식(커넥터), 시작 tree `d4a6d2c`(fa9da73), 22줄(수정 15·추가 5·삭제 2).
  - 임시 index로 목표 tree 재현을 미리 검증했다.
  - 이번 지시문부터 삭제 줄을 `git rm`으로 적었다. 지난번 편집기가 삭제 줄을 건너뛴 일이 있었다.
- Sites 커밋·버전·deployment id: 편집기의 최종 보고를 받지 못했다(not_run). 운영 `/api/version` build `2026-09-25T13:14:48.400Z`로 게시를 확인했다.
- D1 migration: 없음. 새 필수 환경변수 없음. 의존성 파일 변경 없음.
- 봉인 케이스: 게시 시점에 가져오기 전이었다(운영 평가 케이스 dev 74건). 봉인 생성은 이제 e8bd8e0 체크아웃에서 해야 한다(`generator.tree` = `d8eacc9…`).

## 결과
| 단계 | 결과 | 근거 | 증거 |
|---|---|---|---|
| `/api/version` tree 비교 | passed | real | tree `d8eacc9…` = e8bd8e0 tree, build 13:14:48 UTC. 소유자 세션, 13:48 UTC |
| `/api/auth` | passed | real | mode=email, role=owner |
| 익명 업무 API | passed | real | 쿠키 없는 `/api/workspace`·`/api/eval`·`/api/version`·`/api/franchise` → 401 |
| 공개 미디어 경로 | passed | real | `/media/<없는 해시>.png` → 404, `Content-Security-Policy: default-src 'none'`, `Cache-Control: no-store` |
| 백그라운드 작업자 | passed | real | registered·activated·online, lastSeen 13:48 UTC, blocked 0 |
| 가맹 기능 스위치 | passed | real | `r_franchise` enabled false(기본) |
| 평가 연결 점검 | passed | real | `check_connection` 200, status ready |
| 같은 저울 재채점 2 run | passed | real | `regrade_run` 200(토큰 0), 저울 `…+local-rerun+contract-read` / 사전 `.13` |
| 위조 헤더 401 | not_run | | 자동 모드 안전 검사 정책상 보내지 않는다 |
| e8bd8e0 main CI | passed | real | CI success |

- 배포 상태: `runtime-verified` (2026-09-25 13:48 UTC)
- 롤백 대상: 코드 문제면 `fa9da73`을 파일 목록 방식으로 재게시한다.
- 게시 뒤 감시(결정 4): 24~72시간 동안 `invalid_output` 비율, 5xx, Workers CPU를 본다. #112가 `invalid_output`을 줄여야 한다.

## 게시 뒤 재채점 (real, 토큰 0)
| run | 케이스 | 이전(저울 `…+r3-measure`, 사전 `.11`) | 새 저울(`…+contract-read`, 사전 `.13`) |
|---|---|---|---|
| R3 dev 기준선 `eab8911d` | 74 | 650/12, 예방 fail 4, block 0, warn 4 | 652/10, 예방 fail 3, block 0, warn 1 |
| 로컬 채널 재실행 `e788c058` | 6 | 64/6, 예방 fail 0, warn 2 | 68/2, 예방 fail 0, warn 0 |

- R3에서 바뀐 것
  - `contract_json`이 2에서 1이 됐다. MAPDAL 크리에이티브는 pass다. S8 총괄은 원문 여분 괄호라 설계대로 fail이다.
  - `heading_nesting`이 1에서 0이 됐다. MAPDAL 크리에이티브의 연쇄 실패가 풀렸다.
  - 남은 warn 1건은 조건 없는 `[가격 확인 필요]` CTA다. 설계대로 남긴 경고다.
- `e788c058`에 남은 실패 2건은 S8 로컬 채널이다. 실제 결함이고, #111이 고쳤다(아래 재실행).

## S8 로컬 채널 재실행 (real)
| run | 역할 | 토큰 | `local_channel_coverage` | 다른 채점 fail | 예방·규제 |
|---|---|---|---|---|---|
| `93a7c33a` (13:49~13:51 UTC) | 총괄 | 11,985 | pass (1/4 → 4/4) | `industry_metric_leak` | 0 / 0 |
| `480861d6` (13:52~13:54 UTC) | 전략 | 12,702 | pass (1/4 → 4/4) | `industry_metric_leak` | 0 / 0 |

- #111이 S8을 고쳤다. 네이버 플레이스는 채택, 당근은 후순위, 배달앱은 제외로 이유와 함께 결정했다.
- 두 건의 `industry_metric_leak`는 측정 오탐이다. 교육 업종 캠페인에서 현장 스킬이 쓰게 한 배달앱 제외 줄을 요식업 지표 유출로 봤다.
  - 총괄 원문: '- 배달앱: 제외. … 배달 주문 흐름과 맞지 않는다.'
  - 전략 원문: '| 배달앱 | … | 제외 |'
  - PR #113(`+channel-decision`)이 고친다.
- 첫 run `93a7c33a`는 예산 60,000으로 두 건을 넣었다. 전략이 `not_run`이 됐다. 역할 케이스 예약량이 50,000이라, 총괄이 쓴 뒤 남은 48,015로는 제출되지 않았다(설정 실수). 전략만 `480861d6`으로 다시 돌렸다.
- 9월 누적 사용량: 1,935,242 / 2,400,000.
