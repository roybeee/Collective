# 2026-09-24 df7e253

- GitHub 커밋: `df7e25386e06826d0ff40ac96a488d19a8539450` (`merged`, #76). 묶음 4: 지난 게시 aa999c6(Sites 버전 26) 이후 18커밋, #59~#76(#66·#71·#72는 문서만).
- 소스 트리: `0d074b53bbd39c32c233d48ce39be2e4a7e3e18f`
- 승인: 대표가 선택지 질문에 'df7e253 게시 승인'으로 답했다(2026-09-24 13:40 UTC). 앞서 승인한 `2e7a4bb`은 두 번 시도했지만 Sites 작업 사본의 GitHub fetch가 시간 초과로 멈춰 게시하지 않았다. 대표 결정('2번으로 진행하라')으로 #75·#76을 더해 새 SHA로 다시 승인받았다.
- 게시 경로: Sites 작업 환경에서 `git ls-remote`(작은 요청)는 성공했지만 코드 전송(`git fetch`, 얕은 fetch 포함)은 25~90초 시간 초과가 반복됐다. 대표 지시('무조건 해결하라')로 fetch 없이 게시했다. Sites 편집기의 GitHub 커넥터가 aa999c6→df7e253 변경 파일 126개(추가 49·수정 74·삭제 3)를 하나씩 받았다. 파일마다 `git hash-object`를 Claude가 준 기대 blob 해시와 대조했다. `git write-tree`가 목표 tree와 같을 때만 커밋·빌드·게시하도록 지시했다. 게시 에이전트 보고: 파일 적용 추가 49·수정 74·삭제 3, write-tree `0d074b53…` = 목표 tree.
- 게시자/도구: 대표가 Sites 편집기 대화에 Claude가 만든 지시문을 붙여 넣었다. 파일 목록·기대 해시·검증 단계가 지시문에 들어 있다. 환경변수·접근 설정·D1 데이터는 바꾸지 말라고 지시했다.
- Sites 커밋 `16023574756ae6fdaf41110ab870f737d8afaf28` / Sites 버전 27 / deployment `appgdep_6ab5370f6cd08191adcbbd91afe397c7` / 상태 succeeded
- 소요 시간: 약 6분 12초(편집기 보고)
- D1 migration: 없음. 새 필수 환경변수 없음. 의존성 파일(`package.json`·`pnpm-lock.yaml`) 변경 없음.
- 사전 점검: 크레딧·주간 한도 수치는 기록 없음. 실패한 fetch 시도 세 번(2e7a4bb 첫 시도 약 1분 30초, 2e7a4bb 재시도 약 32분 뒤 대표 중지, df7e253 얕은 fetch 약 5분 30초)도 크레딧을 썼다. df7e253 시도 전에 따로 한 연결 확인(`git ls-remote`, 약 50초)은 성공했다.

## 결과
| 단계 | 결과 | 근거 | 증거 |
|---|---|---|---|
| 파일 적용·트리 대조 | passed | real | 게시 에이전트 보고 write-tree = 목표 tree |
| 설치·빌드 | passed | real | 게시 에이전트 보고. 운영 `/api/version` build `2026-09-24T14:41:45.437Z` |
| 게시 | passed | real | 버전 27, deployment succeeded |
| `/api/version` tree 비교 | passed | real | 응답 tree `0d074b53bbd39c32c233d48ce39be2e4a7e3e18f` = df7e253 tree. `promptManifest` null. 소유자 이메일 세션, 2026-09-24 15:08 UTC 경 |
| `/api/auth` | passed | real | mode=email, role=owner |
| 익명 업무 API | passed | real | 쿠키 없는 `/api/workspace`·`/api/version` → 401 |
| 공개 미디어 경로 | passed | real | `/media/<없는 해시>.png` → 404, `Content-Security-Policy: default-src 'none'`, `Cache-Control: no-store` |
| 조사 워커 | passed | real | online, lastSeen 2026-09-24 15:07 UTC, blocked 0, rotationReady true |
| 위조 헤더 401 | not_run | | 자동 모드 안전 검사 정책상 위조 헤더 요청을 보내지 않는다 |
| df7e253 main CI | passed | real | verify·e2e-smoke success |

- runtime-verified 판정: `df7e253`이 `origin/main`과 같다(게시 확인 시점, `git diff df7e253 origin/main` 0개 파일).
- 배포 상태: `runtime-verified` (2026-09-24 15:08 UTC 경)
- 게시 뒤 감시(결정 4): 24~72시간 동안 `invalid_output` 비율 급증, 5xx 증가, Workers CPU 초과가 없는지 본다. 기능 스위치가 있는 동작은 먼저 스위치로 끈다. 코드 문제면 직전 제품 커밋 `aa999c6`을 재게시해 롤백한다. 재게시도 fetch가 막혀 있으면 이번처럼 파일 목록·해시 방식으로 한다.
- 이번 게시로 운영에 들어간 입력 최소화: 4.4 ①②③④⑤⑦⑧ 전부(#68·#69·#73·#74·#75). 결정 17 AI 표시 확인(#67)도 들어갔다. `AI_COPY_CAPTIONS`는 미설정이라 AI 카피 캡션은 꺼져 있다.
- 운영 앱이 내려주는 조사 서버 설치 파일도 CDP 점검판(#76)이 됐다.
