# 2026-09-23 a67a318 — ODA 실사용 오류 보완

- GitHub 제품 커밋: `a67a318abd024880389f5dde5e8923bc2ca60657` — [PR #15](https://github.com/roybeee/Collective/pull/15) merged.
- 제품 tree: `b57380bb9ade2e2aea7a1ee5fc8fcff92ace6b7f`.
- Sites exact-tree 커밋: `75b7e72b260c9b71ae4dfb42bc9043ffeb2a26a3`.
- Sites 버전21, 환경 revision3, 배포 `appgdep_6ab36c7589b881919fb9302d31532b86` succeeded.
- 빌드 시각: `2026-09-23T06:03:45.935Z`. 게시 완료: `2026-09-23T06:07:04.036193+00:00`.
- 기존 owner-private 접근을 유지하고 사용자 승인 범위의 커밋·운영 적용을 완료했다. Sites hosting connector에 로컬 빌드 archive를 게시했다. Work 크레딧 잔량은 미측정이다.

## 변경과 검증

역할별 결과 계약과 재작성/후속 무효화, 실패 회의 단계 재작성, 서버 단일 진행, 업종·측정 규칙, 사용량 진단을 보완했다. [구현 범위](../ODA-EXECUTION-FIXES.ko.md).

| 검사 | 상태 | 근거 |
|---|---|---|
| GitHub main CI | passed · real | run 35824980676, verify/e2e success |
| 회귀 테스트 | passed · real SQLite/핸들러, mocked 공급자 | 21/21 suites, 집계된 assertions 732 |
| Playwright | passed · real Chromium/로컬 D1, mocked 인증·회의 응답 | 10/10, 31.8초 |
| TypeScript / lint 기준선 | passed · real | exit0 / errors106(기준108), warnings44(기준44); lint zero가 아님 |
| 배포용 빌드 | passed · real | node scripts/run-framework.mjs build, exit0 |
| Sites 게시 | passed · real | 버전21, deployment succeeded |
| 운영 버전 | passed · real | 인증 브라우저 /api/version의 tree가 제품 tree와 일치 |
| 운영 역할 화면 | passed · real | 기존 재질문 Insight를 보완 필요로 표시, 재작성 제공, 다음 역할 차단 |
| 커버리지 비율 | not_run | 비율 측정 미실행 |

코드·보안 검토 후 후속 작업물 무효화와 재작성 UI를 보완했다. 최종 보안 검토에 미해결 HIGH/CRITICAL은 없었다. 직접 origin·서로 다른 실제 사용자 간 격리는 이번 운영 검증에 포함하지 않았다.

## 실제 모델 확인

ODA 캠페인의 고객 인사이트 한 건을 운영 UI에서 재작성했다. **passed · real**: HERMES 호출이 완료되어 필수 산출물 검증을 통과하고 검토 대기 초안으로 저장됐다. 공급자 보고 사용량은 입력19,181 + 출력4,035 = 23,216 tokens, 모델 표기는 `hermes-agent`이며 기반 모델과 실제 비용은 미확인이다. 사용량 화면의 누계는 10회, 230,563 tokens이며 이번 호출이 1회 추가된 것을 확인했다. 이 누계에는 과거 결과 요건 미충족 1회/24,814 tokens가 포함되며, 과거 저장됐던 재질문 두 건을 소급해 invalid_output으로 재분류하지 않는다.

- 고객 상황·대안·장벽, 주장별 근거, 우선 가설·반증 조건·최소 조사/실험이 생성됐다. 기존의 작업 선택 재질문이 반복되지 않았다.
- 생성 원문에서 화덕/wood-fired를 검증된 설비로 단정하지 않았고, 인용 URL은 직접 재검증하지 않았다고 명시했다. 첫 구매일 기준 30일 관찰 완료 고객을 재방문율 분모로 정의했다.
- 기존 인사이트 원문은 이력 화면에 남고 outdated로 전환됐다. 후속 전략도 현재 작업물에서 제외되어 현재 작업물은 총괄·새 인사이트 2건이며 다음 실행은 전략이다.
- 다운로드한 실제 원문은 로컬 `outputs/oda-c107-2026-09-23/insight-repaired.md`에 보존했다(개인 캠페인 자료이므로 Git 제외).
- 과거 회의의 완료 발언 4건, Content 실패와 사용량, 실패 단계 재작성 버튼이 운영 화면에 남아 있는 것을 확인했다. 역할 작업물이 변경됐으므로 그 회의의 과거 스냅샷을 그대로 재실행하지 않았다.

전체 8역할 연속 실행과 회의 재시도 실호출은 이번 확인 범위에 포함하지 않는다. 회의 재시도 로직은 mocked 공급자 회귀/E2E로 검증했다. 기존 브리프 원문을 자동 수정한 것은 아니므로 과거 보관함·화덕·재방문 정의 표현은 별도 검토가 필요하다. 구조 검증 통과는 모든 전략의 사실성·효과성 승인과 다르다.

배포 상태: **merged / published / runtime-verified**. 후속 문서 전용 커밋은 추가 게시하지 않는다. 운영 제품 소스는 위 커밋이다.
