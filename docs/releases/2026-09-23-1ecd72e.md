# 2026-09-23 1ecd72e — 이메일 초대 로그인 게시와 공개 전환

PR #19(`docs/email-auth-release`, 커밋 aa06574·b242019)의 기록을 옮겨 적었다. PR #19는 `main`에 병합되지 않았다. 원래 기록에 없는 값은 "기록 없음"으로 두고 추정하지 않는다.

- GitHub 커밋: `1ecd72e94101e5e3d3a098eb1e86804d70f9d03c` — PR #18 `merged`.
- 소스 트리: `bfbc5b634e5774bdcfa67caeeffd22eacc17968f`.
- Sites 투영 커밋: `486394bb3c18669f55953e768ddd94e5f93ff75d`(`publish: Collective email authentication from 1ecd72e`). 로컬 git 객체에서 소스 트리와 같은 tree임을 확인했다.
- Sites 버전: 기록 없음. deployment `appgdep_6ab378faf33c81919101aff6ff1aef8a`, 환경 revision4.
- 게시 시각: 기록 없음. 투영 커밋 작성은 2026-09-23 06:59:32 UTC, 게시 기록 작성은 07:02 UTC다.
- 빌드 시각: 기록 없음.
- 게시자/도구: 기록 작성자 Codex. 게시 도구는 기록 없음.
- 사전 점검(크레딧·주간 한도·겹치는 HERMES 예약): 기록 없음.
- 소요 시간: 기록 없음.

## 인증 설정과 접근 전환

- 사용자 지정 이메일을 초기 관리자 secret으로 설정하고 기존 owner에 연결했다. 비밀번호와 초기 토큰 원문은 저장소에 저장하지 않았다. 초기 등록 링크는 사용자에게 직접 전달했다.
- 실제로 설정한 환경 키 목록은 기록 없음. 절차는 [이메일 로그인 운영 안내](../EMAIL-AUTH.ko.md) 3~5단계다.
- Sites 접근: 게시 때는 owner-only를 유지했다. 이후 사용자 명시 승인으로 public(접근 설정 revision2)으로 전환했다. 승인 원문과 시각은 기록 없음. PR #19 커밋 b242019가 2026-09-23 07:10:22 UTC에 전환을 기록했다.
- bootstrap 환경 세 항목 제거와 재게시: 기록 없음. 다음 게시(dbf94a5, 버전23) 기록에는 "환경 revision4 유지"라고 적혀 있다.

## 결과

| 단계 | 결과 | 근거 | 증거 |
|---|---|---|---|
| 설치 `pnpm install --frozen-lockfile` | 기록 없음 | | |
| 빌드 `node scripts/run-framework.mjs build` | 기록 없음 | | PR #18 개발 검증의 build는 passed |
| 게시 | passed | real | deployment `appgdep_6ab378faf33c81919101aff6ff1aef8a` |
| `/api/auth` | passed | real | 200, mode=email, user=null |
| 익명·위조 GPT 헤더 업무 조회 | passed | real | 401 |
| 미등록 계정 로그인(native scrypt 실행) | passed | real | 401 |
| 공개 전환 뒤 무자격 GET `/` | passed | real | 200, GPT 게이트·리다이렉트 없음 |
| 공개 전환 뒤 `/api/auth` / 업무 API(익명·위조 헤더) / 계정 API | passed | real | 200(email/null) / 401 / 401 |
| 운영 자료 조회와 `/api/version` tree 비교 | not_run | | 당시 관리자 비밀번호 설정 전 |
| GitHub CI `verify` | passed | real | 모두 passed |
| CI 브라우저, push 실행 35829036803 | passed | real Chromium / 기존10은 mocked 인증, 이메일1은 real 로컬 세션 | 기존10 + 이메일1 |
| CI 브라우저, PR 실행 35829042093 | failed | real | 테스트 서버 종료 뒤 연결 거부, 원인 미확정. 비차단 잡 |

- 배포 상태: `published`. 이 게시 자체의 `runtime-verified` 기록은 없다. 다음 게시 dbf94a5(Sites 버전23)가 이 게시를 대체했고, 2026-09-23 07:58 UTC 경 관리자 이메일 세션으로 버전23의 `/api/version` tree가 dbf94a5 tree와 같음을 확인했다([현재 상태](../STATUS.md)).
- 이후 확인: 관리자 초기 비밀번호 설정과 이메일 로그인이 운영에서 동작한다(계정 1개, 관리자 세션으로 `/api/version` 조회).
- 남은 문제: bootstrap 환경 세 항목 제거·재게시 확인. 실제 Android 기기 이메일 로그인 not_run. 만료 session/token/rate 기록 자동 정리 미구현. 설치 파일 권한 판정 단위를 개인 사용자 단위로 바꾸는 일([개선 계획](../IMPROVEMENT-PLAN.ko.md) PR 1).
