# Android 1.0.0 검증 기록

검증 시각: 2026-09-23 06:11 UTC. 기준 소스: `edd7219ad8c044cb8d0616e4170f81a7901903f6`, 작업 브랜치 `feat/android-app`의 Android 소스.

## 전달 APK

- 파일: `~/Downloads/COLLECTIVE/COLLECTIVE-1.0.0.apk`
- 패키지 `kr.mealzip.collective`, versionCode 1, versionName 1.0.0, minSdk 23 / targetSdk 36.
- 크기 3,798,100 bytes. 전용 RSA 3072 릴리스 키, debuggable=false.
- SHA-256: `95308bc880127904212fda310c965debabdc512348ed113919c1c7eb1b9c7ac2`
- 서명 인증서 SHA-256: `a272cb836a1e1b37eb8a88f3e09fcc01ca1f0b6e9a6b502333cbf10ff4595a6f`
- 원격 웹 변경/게시 없음. Play Store 게시 없음. Android 소스와 서명 APK는 별개로 관리한다.

## 실행 결과

| 검사 | 결과 | 근거 |
|---|---|---|
| `./gradlew :app:testDebugUnitTest` | passed · 16/16, errors/failures 0 | real 병합 Manifest / mocked Robolectric Android |
| 외부 URL·payload 차단 RED/GREEN | passed · 수정 전 12중9 실패 → 수정 후12/12 | mocked Robolectric |
| 필수 Activity 등록 RED/GREEN | passed · 추가4개 실패 → 수정 후16/16 | real 병합 Manifest / mocked Android |
| `./gradlew :app:lintDebug :app:assembleRelease` | passed · exit0 | real 빌드, lint errors0/warnings5 |
| `apksigner verify`, `zipalign -c 4` | passed | real 최종 APK, v1/v2/v3 서명 |
| `adb install --no-incremental -r` | passed | real Android16 API36 ARM64 Pixel7 에뮬레이터 |
| 앱 실행 → Sites 로그인 안내 | passed | real 운영 HTTPS, Custom Tabs 상단 바 |
| Continue with ChatGPT → auth.openai.com | passed | real 로그인 폼, 자격증명 입력 없음 |
| Android back → Sites 복귀·재실행 | passed | real 에뮬레이터 |
| 외부 `https://example.com/` Intent | passed | real 에뮬레이터에서 실제 COLLECTIVE origin만 표시 |
| 코드·보안 리뷰 | passed · 확정 HIGH/MEDIUM 0 | real 소스 및 ABH2.7.2 바이트코드 정적 검토 |
| 실제 Android 로그인 완료·업무 화면 | not_run | 사용자 계정 자격증명을 사용하지 않음 |
| Android 파일 업로드·다운로드 | not_run | 인증된 업무 화면 미진입 |
| 물리 기기·API23 실행 | not_run | 현재 검증 기기는 API36 에뮬레이터 |
| 전체 화면 TWA | blocked | 운영 assetlinks 익명401, 상단 바 폴백 검증 |

초기 에뮬레이터 실행에서 ABH가 요구하는 ManageDataLauncherActivity 누락으로 crash가 발생했다.
실제 라이브러리2.7.2 요구와 병합 Manifest를 대조해 등록하고 회귀 테스트4개를 추가했다. 최종 APK 재설치 후 정상 실행을 확인했다.

Lint 경고5개는 도구/라이브러리 최신 버전 알림3개, dataExtractionRules 권고1개, monochrome icon 권고1개다.
현재 앱은 브라우저에 인증을 위임하며 계정 데이터·토큰을 자체 보관하지 않는다.

로컬 실행 증거: `outputs/android/launch.png`, `login.png`, `external-intent-result.xml`, `signature-verification.txt`.
테스트 XML: `android/app/build/test-results/testDebugUnitTest/`.
서명 키는 Git 밖 `~/.local/share/collective-android/signing/`에만 보관한다.
