# 2026-09-25 73147a9

- GitHub 커밋: `73147a99e6d4645f82149605067ef784858a944a` (`merged`, #101). 묶음 9는 지난 게시 64e51e8(Sites 버전 30) 이후의 #99·#101이다. 제품 코드 변경은 #99 채점 도구(`lib/graders/`)와 #101 트랙 R 가맹 모집 리드 원장(R4a·R1a·R4b, 스위치 `r_franchise` 기본 꺼짐)이다.
- 소스 트리: `076ee70dec46db9fe134784214f03b6f4cfcc875`
- 승인: 대표가 1~7단계(커밋·PR·병합·게시)를 승인했다("1-7 모두진행 6번도 승인할테니 너가게시해줘", 2026-09-25 06시 UTC 경). 게시 실행은 Sites 도구가 있는 편집기에서 대표가 했다.
- 게시 경로: 파일 목록·기대 해시 방식(커넥터)이다. 64e51e8→73147a9 목록은 41줄(추가 21·수정 20·삭제 0)이다. 지시문은 원본을 `curl -o`로 파일에 바로 받고, write-tree 직전 전체 해시 재검증과 빌드 뒤 tree 재확인을 처음부터 넣었다(64e51e8 기록의 교훈). 게시 전에 Claude가 64e51e8 작업 사본에서 같은 절차를 모의 실행해 41개 해시 일치와 write-tree = 목표 tree를 확인했다.
- 게시자/도구: 대표가 Sites 편집기 대화에 Claude가 만든 지시문을 붙여 넣었다. 환경변수·접근 설정·D1 데이터는 바꾸지 말라고 지시했다.
- Sites 버전 31 / deployment `appgdep_6ab62fccc25c8191b854607d2e7469c0` / 편집기 작업 시간 11분 7초
- D1 migration: 없음. 새 필수 환경변수 없음. 의존성 파일 변경 없음.
- 사전 점검: 크레딧·주간 한도 수치는 기록 없음.

## 결과
| 단계 | 결과 | 근거 | 증거 |
|---|---|---|---|
| 파일 적용·트리 대조 | passed | real | 편집기 보고: 41개 파일 blob 해시 일치, 소스 tree `076ee70…` = 목표 tree(대표 화면 캡처, 2026-09-25 08:30 UTC 경) |
| 편집기 검사 | passed | real | 편집기 보고: 타입 검사·린트 기준선·전체 테스트(124 스위트, 8,253 검증)·빌드 통과 |
| 게시 | passed | real | 버전 31, deployment `appgdep_6ab62fcc…` |
| `/api/version` tree 비교 | failed | real | 응답 `{"build":"2026-09-25T08:21:04.563Z","tree":"unknown","promptManifest":null}`. 소유자 이메일 세션(휴대폰 브라우저 주소 직접 열기), 2026-09-25 08:38 UTC 경. build 시각은 이번 게시와 맞다 |
| 기능 확인(보조 증거, tree 대체 아님) | passed | real | `/api/feature-flags`(소유자 세션, 2026-09-25 08:4x UTC 경) 목록에 #101에서 더한 `r_franchise`가 있다(`enabled:false`, `source:default`). 기존 스위치 `online_grading`·`a7_repair_turn`은 켜진 채 유지 |
| 73147a9 main CI | passed | real | verify·e2e-smoke success(run 36104750810) |
| 가맹 화면(스위치 켠 뒤) | not_run | | 스위치 기본 꺼짐. 대표가 `r_franchise`를 켠 뒤 확인 |

- runtime-verified 판정: 검증 실패. 응답 tree가 `unknown`이다(`docs/PUBLISH.ko.md` 5단계: `unknown`·`dirty`는 검증 실패). `vite.config.ts` `sourceTree()`는 빌드 때 `git status`·`git rev-parse`가 예외를 내면 `unknown`을 넣는다(파일이 바뀐 채 빌드했다면 `dirty`였을 것이다). 편집기 에이전트는 빌드 뒤 tree `076ee70…`를 보고했으므로, 배포된 번들을 만든 빌드가 git을 쓰지 못한 환경이었을 가능성이 크다. 편집기 읽기 전용 진단(2026-09-25 08:4x UTC 경): 게시 폴더에서 지금 git은 정상이고(git 2.51.1, HEAD tree `076ee70…`, 작업 트리 깨끗, 폴더 소유자와 실행 사용자 같음) `dist/server/index.js` 수정 시각 08:21:09 UTC는 운영 build 08:21:04 UTC와 맞아 운영 번들은 이 폴더 빌드다. 그러나 `dist`에서 목표 tree 문자열이 검색되지 않는다. 즉 그 빌드 순간에는 git 조회가 실패했다. `.git` 수정 시각(08:41 UTC)이 빌드보다 늦고 당시 실행 기록이 남지 않아 실패 원인은 확정하지 못했다(blocked). `/api/feature-flags`에 `r_franchise`가 보여 #101 코드가 운영에 있는 것은 기능으로 확인했다(tree 신원 검증을 대신하지 않는다)
- 배포 상태: `published` (runtime-verified 아님). 재게시는 하지 않는다: 기능 확인상 #101 코드가 운영에 있고 가맹 기능은 스위치로 꺼져 있다. 다음 묶음 게시에서 tree 신원을 다시 확인한다.
- 재발 방지(다음 게시 지시문부터): 빌드 직전 `git status --porcelain`이 비어 있고 `git rev-parse HEAD^{tree}`가 목표 tree임을 같은 셸에서 확인한 뒤, 그 값을 `COLLECTIVE_SOURCE_TREE`로 넘겨 빌드한다(`vite.config.ts` `sourceTree()`가 git 없는 빌드 환경용으로 받는 값). 빌드 뒤 `dist/server`에서 목표 tree 문자열이 검색되지 않으면 게시하지 않는다.
- 게시 뒤 감시(결정 4): 24~72시간 동안 `invalid_output` 비율 급증, 5xx 증가, Workers CPU 초과가 없는지 본다. 가맹 기능은 스위치가 꺼져 있어 운영 동작에 들어가지 않는다. 켠 뒤 문제가 생기면 먼저 `r_franchise`를 끈다. 코드 문제면 직전 제품 커밋 `64e51e8`을 파일 목록 방식으로 재게시해 롤백한다.
- 이번 게시로 운영에 들어간 것:
  - #99 채점 도구 오탐 수정(규제 사전 `compliance-lexicon-2026-09-25.5`). 운영 역할·회의·브리프 제출 바이트는 같다.
  - #101 트랙 R: `/api/franchise`, '가맹 모집' 화면, 설정 기능표 행. `r_franchise`가 꺼져 있는 동안 쓰기 API는 409이고 메뉴는 보이지 않는다.
