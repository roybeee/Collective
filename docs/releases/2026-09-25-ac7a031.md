# 2026-09-25 ac7a031

- GitHub 커밋: `ac7a0310c2d20614a1c364b86ee53cc3eb04764c` (`merged`, #90). 묶음 7: 지난 게시 443fff4(Sites 버전 28) 이후 #81~#90. 묶음 6 `a41624d`(#83, 2026-09-24 승인)는 따로 게시되지 않았고 이번 게시에 함께 들어갔다.
- 소스 트리: `53492af7dea93d68388b4addcb33558dcf125cac`
- 승인: 대표가 선택지 질문에 'ac7a031 게시 승인'으로 답했다(2026-09-25 02시 UTC 경). 같은 답에서 데이터 처리 E1 확장(G2 회의·브리프 동결, J3 심사 재전송)도 승인했다.
- 게시 경로: 파일 목록·기대 해시 방식(커넥터)이다. 지시문에는 v29(a41624d, 46줄)와 v28(443fff4, 61줄) 두 목록을 담았다. 두 목록 모두 임시 index로 목표 tree 재현을 미리 검증했다. Sites가 v28이었으므로 443fff4→ac7a031 목록(추가 35·수정 26)이 적용된 것으로 본다.
  - 게시 에이전트의 첫 실행 보고는 기록되지 않았다.
  - 같은 지시문을 두 번째로 붙여 넣었을 때, 작업 사본 tree가 이미 목표 tree라 아무것도 바꾸지 않았다. 그때 직전 게시 결과로 아래 값을 보고했다.
- Sites 커밋 `01b6e05760804099af847b9a8c2b83ea8639a702` / Sites 버전 29 / deployment `appgdep_6ab5e9ec3ac4819192483b1e70993816` / 상태 succeeded
- D1 migration: 없음. 새 필수 환경변수 없음. 의존성 파일 변경 없음.

## 결과
| 단계 | 결과 | 근거 | 증거 |
|---|---|---|---|
| 파일 적용·트리 대조 | passed | real | 두 번째 실행 보고: 작업 사본 깨끗, HEAD tree = 목표 tree |
| 설치·빌드·게시 | passed | real | 버전 29, deployment succeeded. 운영 `/api/version` build `2026-09-25T03:25:38.665Z` |
| `/api/version` tree 비교 | passed | real | 응답 tree `53492af7dea93d68388b4addcb33558dcf125cac` = ac7a031 tree. `promptManifest` null. 소유자 이메일 세션, 2026-09-25 03:30 UTC 경 |
| `/api/auth` | passed | real | mode=email, role=owner |
| 익명 업무 API | passed | real | 쿠키 없는 `/api/workspace`·`/api/eval`·`/api/version` → 401 |
| 공개 미디어 경로 | passed | real | `/media/<없는 해시>.png` → 404, `Content-Security-Policy: default-src 'none'`, `Cache-Control: no-store` |
| 조사 워커 | passed | real | online, lastSeen 2026-09-25 03:29 UTC, blocked 0, rotationReady true |
| 같은 저울 재채점(평가 API 쓰기) | passed | real | `regrade_run` 4건 200(토큰 0), [관찰](../observations/2026-09-25-same-scale-regrade.md) |
| 위조 헤더 401 | not_run | | 자동 모드 안전 검사 정책상 위조 헤더 요청을 보내지 않는다 |
| ac7a031 main CI | passed | real | verify·e2e-smoke success |

- runtime-verified 판정: `ac7a031`은 `origin/main`의 조상이다. 그 뒤 main 변경(#91~#93)은 비제품 경로(문서·테스트·`scripts/eval/`)뿐이다.
- 배포 상태: `runtime-verified` (2026-09-25 03:30 UTC 경)
- 게시 뒤 감시(결정 4): 24~72시간 동안 `invalid_output` 비율 급증, 5xx 증가, Workers CPU 초과가 없는지 본다. 코드 문제면 직전 제품 커밋 `443fff4`를 파일 목록 방식으로 재게시해 롤백한다.
- 이번 게시로 운영에 들어간 것:
  - 측정 도구 v2와 같은 저울 재채점(#83).
  - AI 심사 루브릭·보정 통계(J1 #84), 회의·브리프 채점기와 업종 사전(G3 #85).
  - 평가 종류 골격과 월 승인 레코드(Q1·Q2 #86). 역할 start 제출은 `roleSubmission` 공유로 바뀌었고 바이트는 같다.
  - 회의 단계·브리프 평가 종류(G2 #87), 합성 케이스 가져오기(G4 #88).
  - 품질 콘솔의 AI 심사 보정 라벨 화면(J2 #89, 소유자 전용), AI 심사 실행(J3 #90).
- 운영 기능 동작 변경: 품질 콘솔에 소유자 전용 라벨 섹션이 생겼다. 그 밖의 역할·회의·브리프 운영 제출 바이트는 같다(assembly-export·prompt-baseline 불변).
- 운영 HERMES 무응답 한도 300초 변경(2026-09-25 승인)은 대표 실행 대기다(not_run).
