# 2026-09-26 8651021

- GitHub 커밋: `8651021f7ae7403b7cd07ac0ac15061e42fb5f2a` (`merged`, #137). 묶음 14다. 직전 운영 `8f0fb54`(#132 head, Sites 버전 39) 뒤 제품 변경:
  - A3 전체: 카피 팩 v2 계약·채점기(#121), 브랜드 말투 원장·작업물 제안 실험·회의 개선본 카피 팩(#129). 스위치 `a3_copy_pack`·`a3_brand_voice`는 기본 꺼짐이다.
  - A6-1 자료 요청 원장(#130), A6-2 플레이스 대조(#133), A6-3 저장 시 자동 수집(#137). 스위치 `a6_data_requests`·`a6_place_check`는 기본 꺼짐이다.
  - `/api/version/public`(#136): 로그인 없이 `build`·`tree`만 준다.
  - 스위치가 꺼져 있으면 모델 제출 바이트와 기존 응답은 그대로다(각 PR 테스트).
- 소스 tree: `1ac4369e96e69a124f693540eea5df2fee3ccf67`
- 승인: 대표 결정(2026-09-26, "원스탑으로 다 진행하라" — 병합·게시를 개발 도구에 위임). 게시 SHA 선택은 2026-09-25에 위임됐다.
- 게시 경로: 자동 게시([게시 절차 8절](../PUBLISH.ko.md#8-자동-게시-chatgpt-예약-작업)).
  - 요청: PR #138, 라벨 `sites-publish` → 지시문 커밋 push(05:16:35 UTC)
  - 지시문: [`docs/publish/8651021.md`](../publish/8651021.md). 파일 목록·기대 해시 방식이다.
  - 시작 tree `98a5800`(8f0fb54)은 소유자 세션 `/api/version`으로 미리 확인했다. 76줄(추가 27·수정 49·삭제 0).
  - 임시 index 모의 적용으로 목표 tree 재현을 확인했다(passed · real). raw 원본 해시는 76/76 일치했다(passed · real).
- Sites 버전 40 / deployment `appgdep_6ab758798bdc8191b48a26537b9bb472` succeeded / Sites 커밋 `027b2abfc41460d9ce7e79346393e2af63938850`
  - 게시 실행: 약 15분
  - 결과 댓글: 05:32 UTC 전
- D1 migration: 없음. 새 필수 환경변수 없음. 의존성 파일 변경 없음.
- 사전 점검: 크레딧·주간 한도 수치는 자동 게시라 기록되지 않는다(8절).

## 결과
| 단계 | 결과 | 근거 | 증거 |
|---|---|---|---|
| 요청 조건(라벨·본문 파싱·지시문 목표 = `PUBLISH-TARGET`) | passed | real | #138 결과 댓글 |
| main 포함 | passed | real | compare main...8651021 = identical |
| 대상 CI | passed | real | run 36219919890 success |
| 다른 게시 없음 | passed | real | 최신 버전 39 succeeded, 진행 중 게시 없음 |
| Sites 작업 사본 시작 tree | passed | real | `git status --porcelain` 빈 값, tree `98a5800…` |
| 파일 적용·blob 검사 | passed | real | 76/76 일치, 불일치 0, 다시 받은 파일 0 |
| `git write-tree` | passed | real | `1ac4369…` = 목표 tree |
| 설치·빌드 | passed | real | `pnpm install --frozen-lockfile`, `COLLECTIVE_SOURCE_TREE` 빌드 exit 0, `TREE_EMBEDDED`, 빌드 뒤 clean·tree 일치 |
| 게시 | passed | real | `save_site_version` → `deploy_site_version`, Sites 버전 40 succeeded |
| 게시 뒤 설정 | passed | real | public 유지, 접근 revision 2, 환경 revision 4, 환경변수·D1·R2 변경 없음 |
| 공개 `/api/version/public` | passed | real | 개발 도구 curl, 200 `{"build":"2026-09-26T05:29:18.615Z","tree":"1ac4369e96e69a124f693540eea5df2fee3ccf67"}` |
| 소유자 `/api/version` | passed | real | 개발 도구가 소유자 세션 Chrome 확장으로 same-origin fetch. 200, build 같음, tree 일치, `promptManifest` null |

- 배포 상태: `published` (Sites 버전 40). 운영 tree가 `8651021` tree와 일치한다.
- main은 그 뒤 #139(트랙 R R15a-1 순수 모듈, 아직 연결 없음)로 앞서 있다. 그래서 main 전체의 `runtime-verified`는 아니다.
- 소유자 경로 확인 방법(처음 사용): Chrome 확장이 끊겨 있어 claude.ai 탭을 열어 재연결했다. 운영 사이트 탭에서 same-origin fetch로 읽었다. 도구가 40자 해시를 가리므로 페이지 안에서 기대 tree와 비교한 결과만 받았다. 읽기만 했다.
- 롤백 대상: `8f0fb54`(tree `98a5800`).
- 게시 에이전트 참고: 파일 적용 뒤 경로 비교 보조 스크립트에서 셸 문법 오류가 1회 났다. 커밋·빌드 전에 멈췄고, 고친 뒤 76개 해시와 경로를 다시 검증했다.
- 다음: A3 종료 조건 run(골든 v2 dev 2건, 예약 100k·실측 약 30k), A6 종료 조건 real(점포 브랜드 자료 요청 1건이 사실 확정으로 닫힘, 토큰 0). 두 run 모두 스위치를 켜야 한다.
