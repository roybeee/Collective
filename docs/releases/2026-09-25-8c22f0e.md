# 8c22f0e 게시 기록 — Sites 버전 35

확인: 2026-09-25 21:30 UTC, Codex. 이 작업은 기존 게시의 사후 확인이며 새 게시를 실행하지 않았다.

| 항목 | 값 |
|---|---|
| GitHub 제품 커밋 | `8c22f0e8738653698dd4350cd2063e3755340c83` |
| 목표/작업 사본 tree | `02260ba4d4bf50dbd617fd4f6dc2709c5e067cf2` |
| Sites source commit | `6abf9bbb125c92d383fc501aab76c0d31b8f7cd8` |
| Sites 버전 | 35 |
| deployment | `appgdep_6ab68fba2a9881918648291e5ba1e40d` |
| 게시 결과 | `published` · real, Sites deployment `succeeded` |
| deployment updated_at | `2026-09-25T15:15:20.468507+00:00` |
| 환경 revision | 4, 조회값이며 이번 작업에서 변경하지 않음 |
| 실행 검증 | `blocked` · real: 이 세션의 브라우저에서 `/api/version` 탐색이 `net::ERR_BLOCKED_BY_CLIENT` |
| S8 재채점 | `not_run`: 운영 연결 차단으로 쓰기 요청을 보내지 않음 |

## 근거와 한계

- Sites `list_site_versions`의 버전 35 source SHA와 기존 게시 작업 사본의 HEAD가 같다. `git status --short`는 비어 있으며 `HEAD^{tree}`는 위 목표와 같다. 커밋 메시지는 `publish: roybeee/Collective 8c22f0e`다.
- `get_deployment_status`가 위 deployment의 `succeeded`를 반환했다. 저장 버전과 게시 결과가 있다는 뜻이며, 인증된 운영 `/api/version` 응답을 대신하지 않는다.
- 별도 새 탭으로 운영 `/api/version`을 열었으나 클라이언트 차단으로 실패했다. 차단 우회나 운영 인증·환경·D1 변경은 하지 않았다. 이 세션에서는 `runtime-verified`로 표시하지 않는다.
- 최신 GitHub main은 `41ea80d008212b5b28796d4d9e0307013592ccbc`(#117)다. #117에는 제품 변경이 있으므로 `8c22f0e` 게시를 최신 main 전체 게시로 해석하지 않는다.
- S8 run 접두사 `93a7c33a`·`480861d6`의 전체 id를 운영에서 확인한 뒤 `regrade_run`으로 재채점하고 `industry_metric_leak` 결과를 기록한다(모델 토큰 0). 접두사만으로 쓰기 요청을 만들지 않는다.
- `/api/auth`, 작업자 online, feature flags, 24~72시간 오류/CPU 관찰은 이 세션에서 `not_run`이다. 과거 관찰을 최신 성공으로 재기록하지 않는다.
