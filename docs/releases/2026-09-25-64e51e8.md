# 2026-09-25 64e51e8

- GitHub 커밋: `64e51e845d09dda821b4a09cab405f4374e9b616` (`merged`, #98). 묶음 8은 지난 게시 ac7a031(Sites 버전 29) 이후의 #91~#98이다. 제품 코드 변경은 채점 도구뿐이다(`lib/graders/` 4개 파일: #95 합성 판정 라벨·측정 면제, #97 표현 부재 부정, #98 비판 문장의 재방문율 산식·'-지는 않' 부정).
- 소스 트리: `449a67740a878885df1732539ef022b38d5e3c1c`
- 승인: 대표가 선택지 질문에 '64e51e8 게시 승인'으로 답했다(2026-09-25 04시 30분 UTC 경).
- 게시 경로: 파일 목록·기대 해시 방식(커넥터)이다. ac7a031→64e51e8 목록은 21줄(추가 4·수정 17·삭제 0)이다. 임시 index로 목표 tree 재현을 미리 검증했다.
  - 1차 실행은 게시 전에 중단됐다. 21개 파일을 적용한 뒤 `git write-tree`가 `05332bd…`로 목표와 달랐다.
    - 불일치 파일은 `docs/observations/2026-09-25-same-scale-regrade.md` 하나였다. 해시는 `e69de29…`로 빈 파일(0바이트)의 해시다. 저장 직후 해시 검사는 통과했으므로 그 뒤 비워진 것이다.
    - 원인은 확인하지 못했다. 원본은 6,906바이트 UTF-8(LF) 문서다.
  - 이어서 실행한 지시문은 그 파일만 `curl -o`로 파일에 바로 다시 받게 했다. 이어서 21개 전체 해시를 재검증하고, 빌드 뒤에도 tree가 같은지 확인한 다음 게시하게 했다.
  - 다음 게시 지시문에는 다음 두 확인을 처음부터 넣는다.
    - write-tree 직전 전체 해시 재검증.
    - 빌드 뒤 tree 재확인.
- Sites 커밋 `ed7aa13b3416e4c1fad67e4fac839357e9a8805a` / Sites 버전 30 / deployment `appgdep_6ab6007ce11481918f6ae9a5aa250c49` / 상태 succeeded (약 3분)
- D1 migration: 없음. 새 필수 환경변수 없음. 의존성 파일 변경 없음.

## 결과
| 단계 | 결과 | 근거 | 증거 |
|---|---|---|---|
| 파일 적용·트리 대조 | passed | real | 2차 실행 보고: 다시 받은 파일 1개, 21개 해시 재검증, write-tree = 목표 tree, 빌드 뒤에도 tree 일치·작업 사본 깨끗 |
| 설치·빌드·게시 | passed | real | 버전 30, deployment succeeded. 운영 `/api/version` build `2026-09-25T05:01:24.144Z` |
| `/api/version` tree 비교 | passed | real | 응답 tree `449a67740a878885df1732539ef022b38d5e3c1c` = 64e51e8 tree. `promptManifest` null. 소유자 이메일 세션, 2026-09-25 05:39 UTC 경 |
| `/api/auth` | passed | real | mode=email, role=owner |
| 익명 업무 API | passed | real | 쿠키 없는 `/api/workspace`·`/api/eval`·`/api/version` → 401 |
| 공개 미디어 경로 | passed | real | `/media/<없는 해시>.png` → 404, `Content-Security-Policy: default-src 'none'`, `Cache-Control: no-store` |
| 조사 워커 | passed | real | online, lastSeen 2026-09-25 05:39 UTC, blocked 0, rotationReady true |
| 같은 저울 재채점(평가 API 쓰기) | passed | real | `regrade_run` 6건 200(토큰 0), 아래 표와 [관찰](../observations/2026-09-25-same-scale-regrade.md) |
| 위조 헤더 401 | not_run | | 자동 모드 안전 검사 정책상 위조 헤더 요청을 보내지 않는다 |
| 64e51e8 main CI | passed | real | CI success |

- runtime-verified 판정: `64e51e8`은 `origin/main`의 조상이다. 이 기록을 쓸 때 main 헤드가 `64e51e8`이다.
- 배포 상태: `runtime-verified` (2026-09-25 05:39 UTC 경)
- 게시 뒤 감시(결정 4): 24~72시간 동안 `invalid_output` 비율 급증, 5xx 증가, Workers CPU 초과가 없는지 본다. 이번 게시는 채점 도구만 바꿨다. 코드 문제면 직전 제품 커밋 `ac7a031`을 파일 목록 방식으로 재게시해 롤백한다.
- 운영 기능 동작 변경: 없음. 역할·회의·브리프 운영 제출 바이트는 같다. 바뀐 것은 평가 채점 결과뿐이다.
  - `GRADERS_VERSION` `failure-types-v1+normalized+measure-v2+g3+compound-labels+absent-expr+critique-clause`
  - 규제 사전 `compliance-lexicon-2026-09-25.4`
- 운영 HERMES 무응답 한도 300초 변경(2026-09-25 승인)은 대표 실행 대기다(not_run).

## 게시 뒤 같은 저울 재채점 (real, 토큰 0)

| run | 케이스 | 통과/실패 | 채점기 실패 | 예방 fail | block | warn | 결함 0개 산출물 |
|---|---|---|---|---|---|---|---|
| 기준선 v1 (`d8da14f1`+`3e63007b`) | 11 | 71/6 (92.2%) | `internal_id_exposure` 6 | 16 | 0 | 9 | 0/11 |
| 품질 수정 v1 뒤 (`5e212bfc`+`12a84ee5`) | 11 | 81/0 (100%) | 0 | 0 | 0 | 4 | **11/11** |
| 파일럿 1 S2 회의 (`fdceaaa7`) | 6 | 52/0 | 0 | 0 | 0 | 0 | 6/6 |
| 파일럿 2 브리프 (`0938a0ca`) | 2 | 17/2 | `internal_id_exposure` 1, `brief_instruction_violation` 1 | 0 | 0 | 0 | 1/2 |

- 예상대로 확인된 것
  - 품질 수정 v1 뒤 결함 0개 산출물이 11/11이다. 앞의 게시(ac7a031) 뒤 재채점에서는 오탐 block 때문에 10/11이었다.
  - 파일럿 1 회의 6단계의 채점기 실패가 0이다. 원래 5건은 모두 오탐이었다.
- 예상과 달랐던 것: 수정 v1 뒤 MAPDAL에 `price_missing` warn 4건이 남았다(ac7a031 재채점 때 9건). #95는 재현한 문장만 고쳤고, 재현하지 못한 문형 4종이 남아 있었다. 원문을 재현한 결과 모두 오탐이다.
  - '이번 목표는 mapdal.kr 온라인 판매이며 …'(목표 서술).
  - 표 머리 '온라인 구매와의 관계'.
  - '온라인 판매 재고와 중복 판매 방지'.
  - 한정 조건 CTA '… 바로 결제 단계로 이동할 수 있을 때만 “구매하기”'.
  - 확인 뒤 고르는 선택지 나열 '… 확인한 뒤 “상품 자세히 보기”, “상품 선택하기”, “구매하기” 중 하나만 사용한다'.
  - PR `fix/compliance-online-sales-cta`(사전 `.5`)가 고친다.
- 기준선 쪽 block 1(ODA cmo `fake_testimonial`)이 0이 됐다.
  - 원문은 `### 수정·중단 조건` 아래 "[제안] 다음 상황에서는 … 중단한다." 목록의 "허위 후기·위장 후기·추천 조작이 확인된다"다. 중단 조건이라 오탐이었다.
  - #95의 합성 판정 라벨이 이 제목을 규칙 칸으로 보게 되면서 빠졌다. 로컬 재현 결과는 다음과 같다(mocked). ac7a031 사전 `.1`에서는 block이고 현재 사전 `.5`에서는 이슈가 없다.
  - 남은 틈: 같은 목록에 제목이 없으면 여전히 block이다. 리드 문장 '다음 상황에서는 … 중단한다'만으로는 목록 전체를 규칙으로 보지 않는다. 지금 데이터에는 해당 사례가 없다.
