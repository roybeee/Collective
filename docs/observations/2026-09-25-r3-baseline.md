# R3 dev 기준선 — 골든셋 v2 dev 74케이스 (2026-09-25)

결론: dev 74케이스를 1,227,228토큰(추정 1.45M의 0.85배)으로 돌렸고 오류는 0이다. 채점 실패와 규제 판정은 원문을 모두 재현했다. 대부분은 측정 도구 오탐이었고 PR #104와 #103이 고쳤다. 남은 실제 결함은 네 가지다.
1. 로컬 채널 검토 누락(점포 목표 cmo·strategy).
2. 계약 형식(빈 섹션·비 JSON 출력).
3. 브리프의 경로 노출·보호 키 제안·허용 밖 키. #102가 고친다.
4. 역할 원문의 경로 노출(예방). 화면에서는 정규화된다.

회의 재검토의 큰 입력(52k)은 결함이 아니라 상한 설정 문제로 보고, 회의 단계 상한을 64,000으로 정했다.

- 근거: real. 운영 `64e51e8`(Sites 버전 30)에서 소유자 세션으로 `POST /api/eval {action:'start_run', set:'dev'}`를 실행했다. 평가 전용 HERMES 프로필을 썼다.
- run `eab8911d-f514-4907-bbd9-eeb5886ceac1`: 2026-09-25 06:58~09:20 UTC, tokenBudget 1,800,000(건별 승인 smoke_cap), 74/74 완료.
- 예산: 대표 결정(2026-09-25)으로 기준선 달을 9월로 정하고 9월 월 승인 2,400,000을 기록했다(`set_budget_approval`). 9월 누적은 578,945 → 1,806,173이다.
- 원래 저울: `failure-types-v1+normalized+measure-v2+g3+compound-labels+absent-expr+critique-clause` / `compliance-lexicon-2026-09-25.4`(운영 코드).
- 운영 빌드 변경(run 도중): 운영 `/api/version`이 2026-09-25 08:21 UTC 빌드부터 tree `unknown`을 보고한다(버전 30은 05:01 빌드, tree `449a677`). 이 run의 29건(역할 24·회의 5)이 그 뒤에 제출됐다. 저장소에는 이 게시 기록이 없다. 제출 바이트는 같았다고 판단하며 근거는 둘이다.
  - 그 뒤 제출된 S6~S8 역할 24건의 `promptHash`가 생성기가 64e51e8 트리에서 계산한 값과 모두 같다(real).
  - 64e51e8에서 08:21 시점 main(`1e42d85`)까지 역할·회의 조립과 평가 실행 코드(`meeting-input`·`meetings`·`practice`·`campaign-policy`·`ai-context`·`role-instruction`·`eval-*`·`hermes`)가 바뀌지 않았다.
  - 그 뒤 채점된 27건의 규제 사전 버전이 `.5`(#99)이고 채점 버전은 #103 전이다. 그 빌드는 `73147a9`(트랙 R #101)였다. 121791b 게시 때 Sites 작업 사본의 tree가 `076ee70…`(= 73147a9 tree)로 확인됐다. 73147a9는 #99를 포함하고 #102·#103·#104는 없다. tree가 `unknown`이었던 이유(빌드 환경의 git 정보)는 확인하지 못했다.
  - 그래서 기준선 제출은 64e51e8과 같은 조립이다. 운영 채점(run 결과의 채점기 판정)은 그 빌드의 채점기일 수 있어 게시 뒤 재채점으로 맞춘다.

## 케이스 구성 (dev 74)

| 출처 | 역할 | 회의 단계 | 브리프 |
|---|---|---|---|
| 운영 캡처 C1 ODA | 3 | 2(실패한 회의의 완료 단계) | 0 |
| 운영 캡처 C2 MAPDAL | 8 | 6(완료 회의) | 0 |
| 합성 S2 베이커리 재방문(fnb) | 8 | 6 | 1 |
| 합성 S3 코딩학원 상담 리드(education) | 8 | 6 | 2 |
| 합성 S6 무인 보관함(locker) | 8 | – | – |
| 합성 S7 분식 자판기 가맹(fnb, B2B) | 8 | – | – |
| 합성 S8 필라테스 체험(education) | 8 | – | – |
| 합계 | 51 | 20 | 3 |

운영에 브리프 초안이 없어 C1·C2 브리프가 없다. 그래서 설계(72)보다 역할·회의 쪽이 2건 많다.

## 원래 저울 결과 (운영 채점기, 게시 전)

| 종류 | 건수 | 통과/실패 | 예방 fail | block/warn | 결함 0개 | 평균 토큰(입력) | 평균·최장 시간 |
|---|---|---|---|---|---|---|---|
| 역할 | 51 | 455/25 | 3 | 2/12 | 28/51 | 16,028(11,080) | 105초·206초 |
| 회의 단계 | 20 | 148/6 | 0 | 0/3 | 15/20 | 19,153(17,290) | 48초·104초 |
| 브리프 | 3 | 24/4 | 0 | 0/0 | 1/3 | 8,910(5,088) | 82초·99초 |

| 캠페인 | 건수 | 통과/실패 | 예방 fail | block/warn | 결함 0개 | 토큰 |
|---|---|---|---|---|---|---|
| MAPDAL 구매전환(운영) | 14 | 89/7 | 1 | 0/12 | 9 | 345,530 |
| ODA 휘경 오픈(운영) | 5 | 38/0 | 0 | 0/1 | 5 | 105,532 |
| S2 베이커리 재방문 | 15 | 145/3 | 0 | 1/0 | 12 | 214,203 |
| S3 코딩학원 상담 리드 | 16 | 126/15 | 1 | 0/0 | 3 | 218,597 |
| S6 무인 보관함 | 8 | 78/4 | 0 | 0/0 | 5 | 114,537 |
| S7 분식 자판기 가맹 | 8 | 80/1 | 1 | 1/1 | 6 | 116,318 |
| S8 필라테스 체험 | 8 | 71/5 | 0 | 0/1 | 4 | 112,511 |

## 측정 도구 오탐 (원문 재현, PR #103·#104가 고침)
채점기 실패 35건 중 `unsupported_claim_term` 15건과 `fact_conflict` 3건은 모두 오탐이었다. `unconfirmed_value_assertion` 2건, `brief_prohibition_conflict` 1건, 회의의 `internal_id_exposure` 1건도 오탐이다. 규제 block 2건과 warn 대부분도 오탐이다. 문형은 다음과 같다.
- 주의 학습 규칙 이름 인용: '‘할인 마감 문구 주의’ 버전 1'. S3 여러 역할·회의·브리프에서 가장 많이 나왔다.
- 캠페인 목표 원문 인용.
- '무료 여부·무료인가요'.
- '‘…’는 확정 사실이 아니므로·확인되지 않아·후보 사실이고'.
- 부정·보류 서술
  - '‘…’이라고 단정하지 않는다'
  - '…확정된 뒤 별도 승인합니다'
  - '적용하지 않는다'
  - '사용할 수 없습니다'
  - '금지된 “…” 표현'
- 금지 목록
  - 제목('### 금지·보류 표현')
  - 리드('다음 표현은 … 쓰지 않는다')
  - 중단 조건 리드('다음 상황에서는 … 중단한다')
- 규제 문형
  - 예산 칸 '유료 광고비 0원'
  - 확정 가격 자리 '[확정 가격]·[가격·통화]'
  - 퍼널 단계 나열
  - 'AI 소재·광고 표시' 점검 행
  - 광고 표시 추가 계획
  - 실험 고정 변수의 '배우 … 게시 시간'
  - '온라인 판매' 명사(#99)
- 회의 원문 경로: 화면은 정규화하는데 평가는 원문을 채점했다(#103: 정규화본 채점, 원문은 예방 판정).

## 새 저울 재채점 (real, 토큰 0, 121791b 게시 뒤)
저울: `…+meeting-normalized+r3-measure` / `compliance-lexicon-2026-09-25.10`. 2026-09-25 10:2x UTC `regrade_run`.

| 항목 | 원래 저울 | 새 저울 |
|---|---|---|
| 통과/실패 | 627/35 | 650/12 |
| 예방 fail | 3 | 4(회의 원문 경로 1건이 예방으로 옮겨감) |
| 규제 block / warn | 2 / 15 | 0 / 9 |
| 채점기 실패 내역 | `unsupported_claim_term` 15 등 | `local_channel_coverage` 6, `contract_json` 2, `heading_nesting` 1, 브리프 3(`internal_id_exposure`·`brief_contract`·`brief_instruction_violation`) |

- 남은 채점기 실패 12건은 모두 아래 '실제 결함'이다.
- 남은 warn 9건(MAPDAL 역할 7·ODA 인사이트 1)은 원문 재현상 대부분 오탐이다. 퍼널 단계 명사 '장바구니'와 확인 뒤 검토·결정하는 CTA가 해당하며, PR #107(사전 `.11`)이 고친다.
- MAPDAL 전략의 조건 없는 'CTA: “구매하기” [가격 확인 필요]'는 품질 수정 v1 결정대로 경고다.

## 실제 결함 (기준선에 남김, 품질 수정 후보)
| 결함 | 케이스 | 비고 |
|---|---|---|
| `local_channel_coverage` 1/4 | S2 총괄·전략, S6 총괄·전략, S8 총괄·전략 | 점포 목표 캠페인이 로컬 채널 4개(플레이스·당근·배달앱·카카오)의 채택·제외 결정을 적지 않음. 6건 |
| `contract_json` | MAPDAL 크리에이티브(빈 섹션), S8 총괄(비 JSON) | 계약 형식 |
| `heading_nesting` | MAPDAL 크리에이티브 | 계약 밖 제목 |
| 브리프 `internal_id_exposure`·`brief_contract`(허용 밖 키 `contextUsed`)·`brief_instruction_violation`(보호 키 `operations`) | S3 브리프 1·2 | #102가 지시문을 고쳤다(운영 미반영이었음) |
| 예방 `internal_id_exposure` | 역할 3건(S3 데이터 등) | 화면은 정규화로 가림, 지시문 준수 결함 |
| `input_budget` 52,268 | MAPDAL 회의 재검토 | 상한을 회의 단계 64,000으로 정함(#104). 결함에서 뺀다 |

## 시간·입력 실측
- 역할 최장 206초(한도 300초). 운영 HERMES 무응답 한도 300초 변경은 대표 실행 대기다.
- 회의 단계 입력: 20단계 중 최대 52,268(MAPDAL 재검토, 개선본 전체와 후보 24,000자), 다음 28,653(MAPDAL 개선본)이고 나머지는 23k 이하다. 합성 S2·S3 회의는 17k 이하다. 운영 회의가 합성보다 크다.
- 역할 입력 최대 23,701(평균 11,080).

## 다음
1. 다음 묶음 `121791b`(#99·#101·#102·#103·#104) 게시 → 이 run `regrade_run`(토큰 0) → 새 저울 수치를 이 문서에 더한다.
2. 브리프 3케이스를 다시 돌려 #102 효과(보호 키 제안 0, 경로 노출 0)를 확인한다(약 27k).
3. R4 대표 라벨: 품질 콘솔 라벨 섹션의 대기열이 65건이다(완료된 active run의 역할 출력, 심사 대상 종류는 역할뿐). 계획은 측정 42·기준 맞춤 10·재라벨 10, 주 20건이다.
4. 품질 수정 후보: 로컬 채널 결정 누락(역할 지시문), 계약 형식.
5. A/A 10건(약 0.22M)은 10월 기본 한도로 돌린다.
