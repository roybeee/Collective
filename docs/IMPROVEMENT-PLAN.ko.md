# 전체 개선 계획

- 사용자 승인: "전체적으로 개선하라"(2026-09-23).
- 기준: `origin/main` `3f68f21`. 운영 제품 `dbf94a5`, Sites 버전23 `runtime-verified`([현재 상태](STATUS.md)).
- 이 문서는 순서·범위·finding 배정만 기록한다. 각 PR의 설계와 검증 결과는 그 PR 본문과 `docs/STATUS.md`에 남긴다.
- 신규 에픽·5단계 로드맵·대표 결정은 [성장 계획](GROWTH-PLAN.ko.md)에 있다. 2026-09-23 배정 변경은 [배정 변경](#배정-변경)에 finding ID로 적는다.

## 결정

| 항목 | 결정 |
|---|---|
| 진행 방식 | 2026-09-23 대표 결정 1로 바꿨다. 파일 소유 표 기반 병렬 레인(코딩 2레인 + PR 6 전담 1)으로 진행한다. 한 PR은 여전히 최신 `origin/main`에서 만든 한 브랜치이고, 공유 파일은 [파일 소유 표](GROWTH-PLAN.ko.md#병렬-레인과-파일-소유-규칙)의 순서대로 병합한다. 이전 결정: "7개 PR(PR 0~6)을 아래 순서로 하나씩 진행한다." |
| 저장소 공개 | 공개를 유지한다. 운영 서버 접속 정보·개인 실명 같은 민감 정보만 제거한다. |
| PNG 게시 이미지 | 외부 공개 호스트에 다시 올리지 않는다. 앱 자체 공개 주소로 제공한다. |
| 운영 게시 | `merged`는 게시가 아니다. 각 PR의 운영 게시는 병합 뒤 별도 승인을 받아 [게시 체크리스트](PUBLISH.ko.md)로 진행한다. 2026-09-23 대표 결정 4로 승인 단위를 묶음으로 바꿨다. 보안은 병합 즉시, 나머지는 단계당 1회 묶음으로 게시한다([묶음 게시 승인](PUBLISH.ko.md#묶음-게시-승인)). |

## 감사 요약

- 범위: 운영 관찰(2026-09-23, 관리자 이메일 세션, 읽기 전용)과 코드 8관점. 관점별 finding ID 접두사는 실행 루프 `exec-loop`, 인증 `auth`, AI 품질 `ai-quality`, 데이터 정합성 `data-truth`, 화면·UX `ux`, 조사 서버·운영 보안 `security-ops`, 측정·학습 루프 `loop`, 엔지니어링 `eng-hygiene`이다.
- 결과: 95건 중 94건 confirmed, 1건 refuted(`data-truth-6`).
- 검증 후 심각도(confirmed 94건): high 3 · medium 46 · low 45. high는 `exec-loop-2`(PR 3), `ai-quality-1`(PR 2), `security-ops-2`(PR 6)다.
- 감사 원문의 근거와 재현 경로는 운영 서버·권한 세부를 담고 있어 공개 저장소에 싣지 않는다. 아래에는 ID와 범위만 적는다.

## PR 순서와 범위

| PR | 이름 | 건수 | 배정 finding |
|---|---|---|---|
| 0 | 엔지니어링 기반 | 6 | `eng-hygiene-1` `-2` `-3` `-4` `-7` `-10` |
| 1 | 보안·인증 | 15 | `auth-1`~`auth-12`, `security-ops-8` `-9`, `eng-hygiene-6` |
| 2 | AI 품질 루프 | 15 | `ai-quality-1`~`-8` `-10` `-11` `-12`, `data-truth-1` `-2` `-5` `-10` |
| 3 | 첫 게시 경로 | 16 | `exec-loop-1` `-2` `-3` `-5` `-6` `-7` `-8` `-9` `-11` `-12`, `data-truth-3` `-4` `-7` `-12`, `security-ops-10`, `eng-hygiene-5` |
| 4 | 측정·비용(4a·4b로 분할, [배정 변경](#배정-변경)) | 17 | `loop-1`~`loop-11`, `exec-loop-4` `-10`, `security-ops-5` `-6` `-11`, `ai-quality-9` |
| 5 | 화면·UX | 15 | `ux-1`~`ux-12`, `data-truth-8` `-9`, `eng-hygiene-12` |
| 6 | 조사 서버 보안(2단계 병행, [배정 변경](#배정-변경)) | 6 | `security-ops-1` `-2` `-3` `-4` `-7` `-12` |

배정 합계 90건, 제외 4건, refuted 1건으로 95건이다. `eng-hygiene-12`는 `ux-5`와 같은 설정 기능 표 문제라 PR 5에 둔다. PR 0은 제품 동작을 바꾸지 않으므로 이 항목을 맡지 않는다. `eng-hygiene-5`(PNG 자산 라우트·사실 카드 렌더·email 모드의 테스트 도달성)는 PR 3이 PNG 제공 경로를 앱 자체 공개 주소로 바꾸므로, 바뀐 경로의 테스트와 함께 PR 3에서 맡는다.

### PR 0 엔지니어링 기반

CI 차단 검사 보강(빌드·워커 계약 테스트 차단, 스위트 시간 제한, `main`의 e2e 결과 기록, STATUS 운영 제품 커밋의 조상 점검은 비차단), 워커 계약 테스트 간헐 실패 수정, lint 오류 감축과 기준선 하향·감소 시 기준선 갱신 강제, 운영 상태 문서 정합화와 `runtime-verified` 정의 수정(비제품 경로 목록), 이 계획 기록. 제품 동작 변경 없음.

제품 동작이 바뀌어 뒤 PR로 넘긴 부분: `eng-hygiene-7` ③ CampaignTable 분리(렌더마다 재마운트)는 PR 5. PR 0 병합 뒤 사용자 승인을 받아 PR #19를 닫는다(`eng-hygiene-1` ①, [현재 상태](STATUS.md)).

### PR 1 보안·인증

대표(최초 관리자)와 일반 관리자의 권한 분리, 인증 모드 기본값 정리, 로그인 제한 방식 보완, 설치 파일 권한을 개인 사용자 단위로 판정, 직원 권한 범위와 행위자 기록, 계정 복구·역할 변경·세션 관리, 만료 인증 기록 정리, 인증 전환 기록 보완, 소스·화면의 운영 서버 접속 정보와 개인 실명 제거.

### PR 2 AI 품질 루프

수정 요청 메모와 직전 실패 사유를 재작성 입력에 반영, 기본 금지 규칙(되묻기·미확인 광고 표현), 미확인 브랜드 주장 표시, 재질문·빈 산출물 차단 강화(회의 경로 포함), 회의 발언 참조 검증 완화, 품질 검수 판정의 후속 연결, 확정·거절 사실과 조사 진단을 AI 입력에 전달, 산출물의 내부 ID 노출 제거, AI 팀 실행의 도구 권한 축소.

### PR 3 첫 게시 경로

사실 원장 초기 입력 경로, 앱 자체 공개 주소로 PNG 제공, 예약 게시와 사실 철회·만료·브리프 변경 맞추기, 막힌 이유 표시와 연결 단계 단순화, 발행 소재와 AI 산출물·기획 승인 연결, 발행 상태 복구, 캠페인–지점 연결, 예산·일정 확인, 사실 key 정리, 소재 보관 정리, PNG 자산 라우트 소유자 격리·사실 카드 렌더 적격성·email 모드 스위트 테스트(`eng-hygiene-5`).

### PR 4 측정·비용

게시물–주문 귀속–성과–학습 연결, 성과 자동 수집의 시작·실패 표시·재수집 기간, 모델 비용 상한과 토큰 예산, 사용량 원장의 작업·캠페인 연결과 단가 적용, 반복 입력 맥락 축소, 자격증명 암호화 키 버전·회전, 커넥터 응답 크기 제한과 조사 결과 형식 오류의 내부 예외 메시지 저장·표시 제거(`security-ops-11`, null 원소를 422로 전환하고 `tests/validate.test.mjs`의 알려진 결함 검사를 바꾼다), 학습 규칙 만료 알림, 바이럴 경로와 측정 가능한 채널 맞추기.

### PR 5 화면·UX

URL 라우팅, 발행→주문→성과 화면 연결, 로그인 계정·권한 반영, 로딩·실패와 빈 상태 구분, 설정 기능 표 현재화, 내부 ID·영문 상태 노출 제거, 재실행 영향 설명, 기본 브랜드 하드코딩 제거, 삭제 보호, 캠페인 상태와 대시보드 수치 정의 통일, 중복 요청 제거, `eng-hygiene-7` ③ CampaignTable 분리(재마운트 해소).

### PR 6 조사 서버 보안

조사 브라우저 격리와 쓰기 도구 제한, 설치기 권한 최소화와 설치 파일 정리, 작업자 자격증명 만료·회전과 중단 알림, 기계 엔드포인트 gate 검증, 업로드 형식·추출 텍스트 검증과 원본 정리.

## 제외 항목

| 항목 | 내용 | 제외 이유 |
|---|---|---|
| `eng-hygiene-8` | 한 줄 압축 코드의 포맷 일괄 변경 | 저장소 전체 diff가 생겨 진행 중인 7개 PR과 모두 충돌한다. 각 PR은 고치는 부분의 주변 스타일을 따른다. |
| `eng-hygiene-11` | 로컬 pnpm 버전 불일치로 `pnpm run` 사용 불가 | 원인을 확정하려면 개발 도구 설치를 바꿔야 한다. 명령은 지금처럼 `node`로 직접 실행한다. |
| `data-truth-11` | EAV records의 스키마 버전과 삭제 연쇄 목록 | 레코드 마이그레이션이 필요한 별도 설계 과제다. 검증에서 오동작은 확인되지 않아 low로 낮춰졌다. 2026-09-23 권고 (1) kind 레지스트리만 F4a에서 다시 열었다([배정 변경](#배정-변경)). |
| `eng-hygiene-9` | Android PR #16 | ChatGPT 인증 시절 기준이라 계속할지 닫을지 먼저 정해야 한다. 이 계획에서는 다루지 않는다. |
| (finding 아님) | 실제 Buffer/Instagram 호출 | 연결 계정과 게시 승인이 없다. 개발·검증은 mocked로 하고 실제 외부 게시는 하지 않는다. |

`data-truth-6`(주문 귀속이 성과·학습으로 흐르지 않음)은 refuted다. 주문 장부 → 점포 측정 → AI 입력 경로가 이미 있음을 확인했다. 캠페인별 귀속 요약 표시는 `ux-2`·`loop-3`에서 다룬다.

## 배정 변경

2026-09-23 대표 결정 1(병렬 레인 승인)에 따른 변경이다. 신규 에픽(F·B·A)의 범위·선행·단계는 [성장 계획](GROWTH-PLAN.ko.md)에 있다. 건수는 finding 단위로 세므로 위 합계(배정 90건, 제외 4건, refuted 1건)는 그대로 두고, 바뀐 배정만 여기에 적는다.

| finding | 이전 | 변경 | 이유 |
|---|---|---|---|
| `loop-4` `loop-5` `security-ops-11` | PR 4 | PR 4a(2단계) | 토큰 예산이 새 HERMES 사용(서버 평가·수리 턴)보다 먼저 있어야 한다. `security-ops-11`은 A7과 같은 파일이라 A7 앞에 병합한다. |
| `security-ops-11` 잔여(null 실험 과제 422 전환) | PR 4a | A7 · **완료** | 원인 코드(`lib/deep-research-server.ts` 실험 과제 파서)가 PR 6 앱 측 레인 잠금이라 PR 4a에서 고치지 못했다. PR 4a는 커넥터 응답 한도와 조사 결과 내부 예외 문구 제거만 한다. A7에서 완료했다. 실험 과제 등 목록 원소가 null·비객체이면 TypeError가 아니라 항목 단위 오류로 처리한다. 뼈대 밖이면 그 원소만 빼고 사유를 남기고(부분 구제), 뼈대면 422(`DeepReportShapeError`)다. 같은 결함이던 점포 조사 보고서(`lib/store-server.ts` `parseStoreReport`)의 실행·실험 제안 null·비객체 원소도 목록 형식 오류 422로 바꿨다(점포 보고서는 구제 없음). `tests/validate.test.mjs`의 알려진 결함 검사 3건은 구제·422 검사로 바꿨다([브랜드 심층 조사](BRAND-RESEARCH.ko.md#부분-구제)). 병합·게시 상태는 PR에서 확인한다. |
| `loop-8` | PR 4 | F2(1단계)가 통째로 흡수 | 실행 신원 조인 키가 있어야 PR 2 이후 실행을 역할·버전·모델 단위로 귀속할 수 있다. |
| `loop-3`·`exec-loop-4`의 추적 코드와 귀속 집계 | PR 4 | A4(1단계)로 앞당김. 잔여(`loop-3` 광고비 이관 등, `exec-loop-4` publicationId 연결·게시 후 주문 검증)는 PR 4b | 점포 실측이 게시물 단위 연결보다 급하다. 집계 함수의 정본은 A4다. |
| `loop-1` `loop-2` `loop-3`(잔여) `loop-6` `loop-7` `loop-9` `loop-10` `loop-11`, `exec-loop-4`(잔여) `exec-loop-10`, `security-ops-5` `-6`, `ai-quality-9` | PR 4 | PR 4b(3단계) | 게시물–주문–성과 연결 파이프다. `ai-quality-9`는 B5 맥락 정책 비교표를 설계 근거로 받은 뒤 구현한다. `security-ops-5`·`-6` 키 회전은 F5의 새 자격증명 형식에 적용한다. |
| `data-truth-11` 권고 (1) kind 레지스트리 | 제외 | F4a(1단계)에서 다시 연다 | 성장 계획이 새 kind를 10개 넘게 더해 삭제 뒤 고아 레코드 위험이 커진다. 마이그레이션은 필요 없다. |
| `data-truth-11` 권고 (2) 스키마 버전·마이그레이션 | 제외 | 계속 제외 | 레코드 마이그레이션이 필요한 별도 설계 과제라는 판단이 그대로다. |
| PR 6 전체(`security-ops-1` `-2` `-3` `-4` `-7` `-12`) | PR 5 뒤 마지막 | 2단계(W11–W17)에 별도 에이전트가 병행 | high `security-ops-2`를 16주 넘게 미루지 않는다. 공유 서버 설치기 변경은 대표 확인 뒤 적용한다. |
| `ux-9` 권고 2 잔여(보관 기본값·목록 숨김) | PR 5 | PR 5 후속 서버 레인. PR 5b는 권고 1·3과 권고 2의 제목 입력 확인만 반영 | 2026-09-24. 보관에는 캠페인 archived 상태, 목록·대시보드 필터, 보관 해제 같은 서버 변경이 필요하다. PR 5b는 서버 API·스키마를 바꾸지 않는다. |
| F4a 후속: 학습 화면의 동결 요약(`viral_experiment_summary`) 표시 | PR 5b | 후속(`GET /api/learning` 확장과 함께) | 2026-09-24. 기존 API가 동결 요약을 돌려주지 않아 읽기만으로는 표시할 수 없다. PR 5b는 원 캠페인이 삭제된 보존 규칙의 배지·안내·버튼 숨김만 반영한다. |

- PR 4 분할 검산: F2 1건 + PR 4a 3건 + PR 4b 13건 = 17건이다.
- PR 2(`merged`, #25)가 맡은 `ai-quality-1` `-4` `-10`은 배정을 바꾸지 않는다. B1은 `ai-quality-1`의 저장을 버전별 이력으로 확장하고, F1은 `ai-quality-4`·`-10`의 판정 함수를 import해 재사용한다.
- 공유 파일의 병합 순서는 [파일 소유 표](GROWTH-PLAN.ko.md#병렬-레인과-파일-소유-규칙)가 정본이다.
