import type {Brand,Campaign,Artifact,Run,Metric,Event} from './agency';
export type WorkspaceData={brands:Brand[];campaigns:Campaign[];artifacts:Artifact[];runs:Run[];metrics:Metric[];events:Event[];connection:{configured:boolean;canConfigure:boolean;model:string;provider?:'hermes'|'openai';endpoint?:string};preview:boolean};
export async function api(action:string,payload:Record<string,unknown>={},path='/api/action'){const r=await fetch(path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,...payload})});const data=await r.json() as {id:string;status:string;error?:string;ok?:boolean};if(!r.ok)throw new Error(data.error||'요청에 실패했습니다.');return data}
export function downloadText(name:string,text:string){const a=document.createElement('a');const url=URL.createObjectURL(new Blob([text],{type:'text/markdown;charset=utf-8'}));a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}
