import type {Brand,Campaign,Artifact,Run,Metric,Event} from './agency';
export type WorkspaceData={briefDrafts?:{id:string;status:string;title:string;brandId:string;campaignId?:string;createdAt:string}[];brands:Brand[];campaigns:Campaign[];artifacts:Artifact[];runs:Run[];metrics:Metric[];events:Event[];connection:{configured:boolean;canConfigure:boolean;model:string;provider?:'hermes'|'openai';endpoint?:string};preview:boolean};
export async function api(action:string,payload:Record<string,unknown>={},path='/api/action'){const r=await fetch(path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,...payload})});const data=await r.json() as {id:string;status:string;error?:string;ok?:boolean;researchQueued?:boolean};if(!r.ok)throw new Error(data.error||'요청에 실패했습니다.');return data}
export function downloadText(name:string,text:string){const a=document.createElement('a');const url=URL.createObjectURL(new Blob([text],{type:'text/markdown;charset=utf-8'}));a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}

export function clientId(){if(typeof crypto.randomUUID==='function')return crypto.randomUUID();const bytes=crypto.getRandomValues(new Uint8Array(16));bytes[6]=(bytes[6]&15)|64;bytes[8]=(bytes[8]&63)|128;const h=Array.from(bytes,b=>b.toString(16).padStart(2,'0')).join('');return `${h.slice(0,8)}-${h.slice(8,12)}-${h.slice(12,16)}-${h.slice(16,20)}-${h.slice(20)}`;}
